# PostgreSQL local development configuration
POSTGRES_DB=nexflow
POSTGRES_USER=nexflow
POSTGRES_PASSWORD=nexflow
POSTGRES_PORT=5432

# Use this from a backend running directly on your machine.
DATABASE_URL=postgresql+asyncpg://nexflow:nexflow@localhost:5432/nexflow

# Model provider keys. Keep real values in .env only.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=

# xAI/Grok OpenAI-compatible endpoint.
XAI_BASE_URL=https://api.x.ai/v1
XAI_DEFAULT_MODEL=grok-4.3
AWS_BEDROCK_REGION=us-east-1
