async def web_search(query: str) -> str:
    """Search through the selected provider once credentials are configured."""
    raise NotImplementedError(f"Web search tool is not configured for {query!r}.")
