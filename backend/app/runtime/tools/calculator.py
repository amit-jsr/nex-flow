def calculate(expression: str) -> str:
    """Evaluate expressions after an approved calculator runtime is selected."""
    raise NotImplementedError(f"Calculator runtime is not configured for {expression!r}.")
